# ─── Admin panel env vars — copy to .env.local (never commit) ─────────
#
# Password: webahmad23
# Hash below is bcrypt(password, rounds=12). Regenerate with:
#   node scripts/hash-password.mjs <new-password>
ADMIN_PASSWORD_HASH="$2b$12$MezQviLVTB1XKFmfhExw.OiA56uOU/jADXeftDzZzghmCC9tG882m"

# JWT signing secret — must be long & random. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_JWT_SECRET="CHANGE-ME-paste-a-64-char-random-hex-string-here"

# GitHub Personal Access Token with "Contents: Read & Write" permission on
# the Ahmad-Portofolio repo. Create at: https://github.com/settings/tokens
GITHUB_TOKEN="ghp_..."
GITHUB_OWNER="priaberbahasa"
GITHUB_REPO="Ahmad-Portofolio"
GITHUB_BRANCH="main"
